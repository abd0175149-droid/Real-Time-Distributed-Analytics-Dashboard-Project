# صفحة اختبار نظام التتبع - Analytics Test Landing Page

صفحة Landing Page تفاعلية مصممة لاختبار جميع أنواع الأحداث التي يجمعها نظام التتبع التحليلي.

## 🎯 الغرض

هذه الصفحة مصممة للتأكد من:
- ✅ الاتصال الصحيح بخادم التتبع
- ✅ جمع البيانات بدقة
- ✅ تتبع جميع أنواع الأحداث

## 📊 البيانات المجمعة

### معلومات الجهاز
- نوع الجهاز (Desktop/Mobile/Tablet)
- نظام التشغيل
- المتصفح
- دقة الشاشة
- حجم النافذة

### الموقع الجغرافي
- الدولة والمدينة
- عنوان IP
- خط العرض والطول

### معلومات الشبكة
- نوع الاتصال (4G/3G/etc)
- سرعة التحميل
- RTT

### أداء الصفحة
- وقت تحميل الصفحة
- وقت DNS
- وقت DOM

## 🎮 الأحداث المتتبعة

| الحدث | الوصف |
|-------|--------|
| `page_load` | تحميل الصفحة مع كل البيانات الأولية |
| `page_view` | مشاهدة الصفحة |
| `mouse_click` | النقر بالماوس |
| `button_click` | النقر على الأزرار |
| `link_click` | النقر على الروابط |
| `scroll_depth` | عمق التمرير (25%, 50%, 75%, 100%) |
| `form_focus` | التركيز على حقول النموذج |
| `form_input` | الكتابة في النموذج |
| `form_submit` | إرسال النموذج |
| `video_play` | تشغيل الفيديو |
| `video_pause` | إيقاف الفيديو |
| `video_complete` | اكتمال الفيديو |
| `product_view` | عرض منتج |
| `cart_add` | إضافة للسلة |
| `purchase` | إتمام الشراء |
| `custom_event` | أحداث مخصصة |

## 🚀 كيفية التشغيل

### 1. تشغيل الخادم الخلفي (Backend)

```bash
cd BackEnd
composer install
php artisan serve
```

سيعمل الخادم على: `http://localhost:8000`

### 2. فتح صفحة الاختبار

يمكنك فتح الصفحة مباشرة في المتصفح:

```
file:///path/to/test-landing-page/index.html
```

أو باستخدام Live Server في VS Code.

### 3. مراقبة البيانات

- افتح لوحة التتبع المباشر في أسفل يسار الصفحة
- راقب الأحداث المرسلة في الوقت الحقيقي
- تحقق من البيانات المجمعة في بطاقات المعلومات

## ⚙️ التكوين

كود التتبع مُعد في `index.html`:

```html
<script 
    src="../tracker/index.js"
    data-endpoint="http://localhost:8000/api/track"
    data-tracking-id="trk_TB4ABmsyyoJRpilc"
    data-batch-size="10"
    data-interval="5000"
    data-debug="true">
</script>
```

### الخيارات:
- `data-endpoint`: عنوان API للتتبع
- `data-tracking-id`: معرف التتبع الفريد
- `data-batch-size`: حجم الدفعة للإرسال
- `data-interval`: فترة الإرسال الدوري (بالمللي ثانية)
- `data-debug`: تفعيل وضع التصحيح

## 🧪 اختبار الميزات

### 1. اختبار النقرات
- انقر على الأزرار المختلفة في قسم "اختبار النقرات"
- راقب تسجيل الأحداث في لوحة التتبع

### 2. اختبار الروابط
- انقر على الروابط الداخلية والخارجية
- راقب تتبع نوع الرابط (داخلي/خارجي/تحميل)

### 3. اختبار النماذج
- املأ حقول النموذج
- راقب تتبع التركيز والإدخال
- أرسل النموذج وراقب حدث الإرسال

### 4. اختبار الفيديو
- شغل الفيديو وأوقفه
- راقب تتبع نسب المشاهدة (25%, 50%, 75%)

### 5. اختبار التجارة الإلكترونية
- اعرض المنتجات
- أضف منتجات للسلة
- أكمل عملية الشراء

### 6. اختبار التمرير
- مرر الصفحة لأسفل
- راقب تتبع المعالم (25%, 50%, 75%, 100%)

## 📁 هيكل الملفات

```
test-landing-page/
├── index.html      # الصفحة الرئيسية
├── styles.css      # أنماط CSS
├── app.js          # منطق JavaScript
└── README.md       # هذا الملف
```

## 🔧 استكشاف الأخطاء

### لا يتم إرسال البيانات؟
1. تأكد من تشغيل خادم Laravel على `localhost:8000`
2. تحقق من Console في المتصفح للأخطاء
3. تأكد من تفعيل CORS في الخادم

### لا تظهر بيانات الموقع؟
- خدمة GeoIP تتطلب اتصال بالإنترنت
- قد تكون الخدمة محظورة في بعض الشبكات

### الفيديو لا يعمل؟
- الفيديو التجريبي من W3Schools
- تأكد من اتصال الإنترنت
